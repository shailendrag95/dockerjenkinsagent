pipeline{
    agent{
        Dockerfile true
    }
    stages{
        stage("Dockerfile running"){
            steps{
                echo "========executing the process========"
            }
        }
    }
}