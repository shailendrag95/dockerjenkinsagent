pipeline{
    agent{
        dockerfile true
    }
    stages{
        stage('Build'){
            steps{
                echo "========executing the process========"
                sh 'node --version'
                sh 'pwd'
            }
        }
    }
}